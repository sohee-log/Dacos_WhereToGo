# C(프론트·배포)가 해야 할 것 — 2026-08-31 기준

> 작성 B(추천 엔진) · 받는 사람 C(프론트·배포)
> 배포·DB는 **실제로 호출해서** 확인한 값이다. 계약 원문은 [`HANDOFF_TO_C.md`](HANDOFF_TO_C.md),
> 전체 상황은 [`BRIEF_2026-08-31.md`](BRIEF_2026-08-31.md).
> 이전 판(8/28)의 C-1·C-2·C-3·C-4·C-5는 **전부 해소됐다.** 지운다.

---

## 먼저 — 8/28 지적사항이 전부 처리됐다

| 8/28에 없던 것 | 지금 |
|---|---|
| Render `DATABASE_URL` 미설정 | ✅ `{"db": true, "mode": "live"}` |
| `LLM_MODEL` 404 | ✅ 설명이 실제로 나온다 (`llm` → 재요청 시 `cache`) |
| 사후 만족도 UI | ✅ `ResultCard` 별점 — 만족도 **24건** |
| `user_id` localStorage | ✅ `lib/session.ts` · URL → 로컬 순으로 읽는다 |
| `docs/scenarios.md` | ✅ 20개 |

**`/onboarding/party` 단계를 새로 만든 것**은 특히 좋은 판단이다. `party_size`가
사용자에게 묻지도 않고 2로 나가던 것을 한 스텝 더 받아 해결했다. `party_size`는
프로필이 아니라 occasion이라 온보딩 5문항에 넣지 않은 것인데, 그 사이 틈을 정확히
메웠다.

**M7 폐루프가 닫혔다** — 로그 809건 · 사용자 55명 · 만족도 24건.

---

## 🔴 C-1 Render가 **#43에서 멈춰 있다** — 대시보드에서 한 번 눌러야 한다

자동 배포가 **한 번만** 걸렸다. 배포본에는 #43(결과 순서 버그)까지만 들어가 있고
**#44 · #46(성능)은 반영되지 않았다.** 그 뒤로 40분을 더 기다려도 그대로다.

실측 (2026-08-31 00:30)

| 확인 대상 | 값 | 판정 |
|---|---:|---|
| 추천 결과 점수 순 | ✅ 내림차순 | **#43 반영됨** |
| `GET /` (DB 0회) | 143ms | 전송 바닥 |
| `GET /health` (DB 1회) | **433ms** | **#44 미반영** |
| 추천 (캐시 히트) | **3,257ms** | **#44·#46 미반영** |

**Render 대시보드 → `wheretogo-api` → Manual Deploy → Deploy latest commit**
(Settings → Build & Deploy → Auto-Deploy가 Yes인지도 함께 본다)

### 배포됐는지 확인하는 법 — `/health` 하나면 된다

성능 PR이 들어가면 DB 체크아웃이 왕복 3회에서 1회로 줄어든다. 브라우저로
[`/health`](https://dacos-wheretogo.onrender.com/health)를 두어 번 새로고침하며
개발자도구 Network 탭의 시간을 본다.

```
옛 빌드  /health 약 430ms  ·  추천 약 3.3초
새 빌드  /health 약 230ms  ·  추천 1초 안팎
```

**점수 순서로는 더 이상 구분할 수 없다.** #43은 이미 배포본에 들어가 있어서,
`?debug=1`을 켜면 지금도 1번 카드 점수가 가장 높다.

### 배포되면 무엇이 달라지나

**응답이 3.3초 → 1초 안팎.** DB 왕복 횟수를 10회에서 6회로, 체크아웃당 왕복을
3회에서 1회로 줄였다. **화면 코드는 고칠 게 없다.**

---

## 🟡 C-2 리허설 2회 — M8의 마지막 칸

**실제 배포본으로** 두 번 돈다. 아래를 눈으로 확인한다.

```
□ 온보딩 5문항 → 인원 → 추천 → 카드 클릭 → 별점까지 끊김 없이
□ ?debug=1 에서 1번 카드의 score가 가장 높다              ← #43 (이미 배포됨)
□ 추천 응답이 1초 안팎이다                                ← #44·#46 (재배포 필요)
□ 같은 좌표로 시각만 바꿔 결과가 실제로 달라진다           ← 차별점 시연
□ 이태원/용산역 카드에는 live_segment·crowd 가 숫자로 뜬다
□ 후암/이촌/청파 카드에는 그 두 키가 아예 없다 (0으로 그리지 않는다)
□ 첫 요청이 콜드스타트여도 안내 문구가 뜬다
```

**세 번째 줄이 이 프로젝트의 시연 포인트다.** 같은 자리에서 13시와 19시를 넣어
결과가 바뀌는 것을 보여주는 것이 "지금의 상황에 맞는 추천"의 증거다.
네·다섯 번째 줄은 `scenarios.md`가 경고한, 가장 틀리기 쉬운 자리다.

---

## 🟡 C-3 캐시 워밍은 **발표 전날 밤에 한 번 더** (B가 이미 한 번 돌렸다)

```powershell
cd roleB
python -m tools.warm_cache --url https://dacos-wheretogo.onrender.com
```

방금 돌린 결과 — 마지막 회차 100건 중 **80건이 캐시에서** 나왔다(나머지 20은
탐색 슬롯이라 원래 template이다). `explanation_cache` 75행 · 누적 히트 177.

**그런데 전날 밤에 한 번 더 돌려야 한다.** 캐시 키에 `weather_state`가 들어가
있어서 날씨가 바뀌면 오늘 데운 것이 내일은 미스가 된다. 4~5분이면 끝난다.
이게 되어 있으면 **발표 중 LLM 호출이 0회**가 되어 쿼터·네트워크 사고가
원천 차단된다.

---

## 🟡 C-4 UptimeRobot (C2-5) — 미확인

Render Free는 15분 무접속이면 슬립하고 깨어나는 데 **1분쯤** 걸린다. 발표 첫
요청이 콜드스타트면 그 1분을 무대에서 기다리게 된다.

`keepalive.yml`이 있지만 **GitHub Actions 스케줄은 믿을 수 없다** — A의 폴링이
같은 이유로 두세 시간에 한 번밖에 안 돈다(TODO_FOR_A 참조). UptimeRobot 같은
외부 핑이 확실하다. 5분 간격으로 `/health`.

**최소한의 보험** — 발표 시작 10분 전에 `/health`를 한 번 열어 둔다.

---

## 🟢 C-5 README + 데모 GIF (C6-5) — 포트폴리오용

발표에 필수는 아니다. 여유가 있으면.

---

## 요약 — C가 발표 전에 할 것

| 순 | 할 일 | 걸리는 시간 |
|---|---|---|
| 1 | **Render Manual Deploy** | 버튼 하나 |
| 2 | 리허설 2회 (위 체크리스트) | 30분 |
| 3 | 발표 전날 밤 캐시 워밍 | 5분 |
| 4 | 발표 10분 전 `/health` 한 번 | — |

**1번이 제일 급하다.** 그게 안 되면 2번 리허설이 옛 빌드를 검증하는 게 된다.
